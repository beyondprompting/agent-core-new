/**
 * @deprecated Este archivo está deprecado. Usa /config/tenant.config.ts en su lugar.
 * 
 * Re-exportando desde la nueva ubicación para mantener compatibilidad.
 */

export { clientConfig, tenantConfig, ACTIVE_TENANT } from "@/config/tenant.config";
export type { TenantConfig } from "@/config/tenant.config";
